import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({ selector: 'app-tests', standalone: true, imports: [RouterLink], templateUrl: './tests.component.html' })
export class TestsComponent {}
