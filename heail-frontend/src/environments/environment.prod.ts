// Production values — this file replaces environment.ts at build time for the
// `production` configuration (angular.json fileReplacements). Update apiBaseUrl here
// if the backend ever moves to a different host/subdomain than the frontend.
export const environment = {
  production: true,
  apiBaseUrl: 'https://heail.in'
};
